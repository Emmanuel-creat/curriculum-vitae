import ProjectPage from "@/components/ProjectPage";

export const metadata = {
  title: "Calendrier BTI · Emmanuel Bailly",
  description: "L'emploi du temps de la promo M2 BTI. Node, Excel, admin CRUD, Render.",
};

export default function Page() {
  return <ProjectPage slug="calendrier-bti" />;
}
